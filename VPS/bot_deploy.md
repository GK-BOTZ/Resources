# Ham Bots 2 Tareeke Se Deploy Karte Hai.

## 1. Python

Python Se Bot Deploy Ke Liye Kuch Extra Packages.

| Package         | Purpose                                      |
|-----------------|----------------------------------------------|
| `screen`        | Bot Hamesha Run Karne Ke Liye     |
| `uv`            | pip jaisa but 10x fast |

### install uv
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

```bash
source ~/.bashrc
```

### install screen
```bash
sudo apt install -y screen
```


## 2. Docker

Docker Se Bot Deploy Ke Liye Hame Bas Docker Chahiye 😆.

### docker install ke liye 

```bash
sudo apt install docker.io
```